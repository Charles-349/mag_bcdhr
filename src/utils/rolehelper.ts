// import { TIEmployee } from "../Drizzle/schema";

// export const normalizeRole = (role: any): TIEmployee["role"] => {
//   if (!role) return "employee";

//   const normalized = String(role).toLowerCase().trim();

//   const allowedRoles = ["admin", "hr", "manager", "employee"];

//   return (allowedRoles.includes(normalized) ? normalized : "employee") as TIEmployee["role"];
// };
// export default normalizeRole;
